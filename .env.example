# fal.ai MCP Server Configuration

# Required: Your fal.ai API key
FAL_API_KEY=your-fal-ai-api-key-here

# Required: Directory for caching model schemas (use absolute path)
FAL_CACHE_DIR=/tmp/fal-cache

# Optional: Download directory for files (defaults to current directory)
# FAL_DOWNLOAD_DIR=/path/to/downloads

# Required: Upload state directory for async uploads
UPLOAD_STATE_DIR=/tmp/fal-uploads

# Optional: API endpoint (default provided)
# API_URL=https://fal.ai/api